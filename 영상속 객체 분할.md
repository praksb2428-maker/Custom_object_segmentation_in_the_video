# 참고 프로젝트 분석 보고서

- 학번: 20221703
- 이름: 박준호

# 1. 개요

## 1.1. 기본 정보

- 프로젝트 또는 논문명: **SAM2: Traffic Accident Video Seg w/Back Removal**
- 연도: **2026년** (참고 Kaggle 노트북 기준, SAM 2 모델은 2024년 공개)
- GitHub 저장소 또는 URL:
  - [참고 Kaggle 노트북](https://www.kaggle.com/code/stpeteishii/sam2-traffic-accident-video-seg-w-back-removal/notebook)
  - [SAM 2 GitHub 저장소](https://github.com/facebookresearch/sam2)
  - [사용 데이터셋](https://www.kaggle.com/datasets/islamalattar/real-time-traffic-accidents)
- 해당 프로젝트 선정 이유: 영상에서 특정 물체를 선택한 뒤, 움직이는 물체만 계속 따라가며 배경을 제거하는 방식이 내가 구현하려는 기능과 비슷하기 때문이다. OpenCV를 이용한 영상 처리 과정도 함께 참고한다.

## 1.2. 문제 정의

- Task 종류: **영상 객체 분할, 객체 추적, 배경 제거**
- 문제 정의: 영상의 첫 화면에서 물체를 선택하고, 다음 장면에서도 같은 물체를 찾아 영역을 분리한다. 선택하지 않은 배경은 검은색이나 투명하게 처리한다.
- 목표: 사용자가 선택한 물체가 움직여도 형태와 위치를 계속 찾아내고, 해당 물체만 보이는 결과 영상을 만든다.

# 2. 프로젝트 분석

## 2.1. 모델 아키텍처

- 사용 모델명: **SAM 2.1 Hiera Large**
- 핵심 알고리즘 및 메커니즘 조사: 첫 프레임에서 점이나 사각형으로 물체를 선택하면 SAM 2가 물체의 영역을 찾는다. 이후 프레임에서도 이전 결과를 참고해 같은 물체를 계속 분할한다.

## 2.2. 요구사항

- 권장/최소 GPU: CPU에서도 실행할 수 있지만 처리 시간이 길기 때문에 CUDA를 지원하는 NVIDIA GPU 사용을 권장한다. 참고 노트북의 전체 실행 시간은 약 52분 29초였으며, 이 중 161개 프레임을 처리하는 데 약 47분 33초가 걸렸다.
- 필요 VRAM 용량: 공식 최소 용량은 공개되지 않았다. 이 프로젝트에서는 원활한 실행을 위해 16GB를 권장하며, 메모리가 부족하면 더 작은 SAM 2 모델을 사용한다.
- 소프트웨어 요구사항: Kaggle Notebook 또는 Linux·WSL 환경, Python 3.10 이상, PyTorch 2.5.1 이상, torchvision 0.20.1 이상, OpenCV, NumPy, Pillow, Matplotlib, SAM 2가 필요하다.

# 3. 데이터셋 분석

## 3.1. 데이터 특징

- 데이터 유형: 교통사고와 비사고 상황을 담은 RGB 이미지 및 MP4 영상
- 해상도 및 클래스 수: 해상도는 파일마다 다르며 Kaggle 설명에 정확한 값이 없다. 폴더는 사고와 비사고로 나뉘지만, 이는 영상 분류용 구분이며 객체 분할 클래스는 아니다.
- 전체 데이터 수: **746개 파일**
- 총 용량: **3.21GB**
- 데이터 분할 비율(Train / Val / Test): 공식 분할 비율은 없다. 이 프로젝트는 학습된 SAM 2 모델을 이용하므로 별도의 학습 데이터 분할은 필요하지 않다. 정확도를 평가할 때는 일부 장면에 정답 마스크를 직접 만들어 평가한다.

# 4. 내 프로젝트 적용 사항

## 4.1. 적용 가능성 검토

- 참고 프로젝트의 한계:
  - 데이터셋에 분할 정답이 없어 정확도를 바로 계산하기 어렵다.
  - 물체를 선택하는 좌표가 코드에 직접 입력되어 있어 영상이 바뀌면 다시 지정해야 한다.
  - CPU로 실행하면 처리 시간이 오래 걸린다.
  - 데이터셋의 라이선스가 `Unknown`이므로 원본 영상의 사용 조건을 따로 확인해야 한다.
- 내 프로젝트 적용점 및 개선 방안:
  - 첫 화면에서 마우스로 물체를 선택하는 기능을 추가한다.
  - OpenCV로 마스크의 작은 구멍과 경계의 잡음을 줄인다.
  - 물체의 중심점을 계산해 이동 경로를 표시한다.
  - 일부 장면에 정답 마스크를 만들어 IoU와 Dice Score(분할 결과의 정확도를 비교하는 지표)로 결과를 평가한다.
  - 배경을 검은색으로 만든 MP4와 투명 배경 이미지를 출력한다.
- 예상되는 문제:
  - 물체가 다른 물체에 가려지거나 화면 밖으로 나가면 추적이 끊길 수 있다.
  - 작은 물체나 빠르게 움직이는 물체는 정확하게 분할되지 않을 수 있다.
  - 고해상도 영상은 처리 시간과 메모리 사용량이 커질 수 있다.
